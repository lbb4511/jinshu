<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jinshu.api.dao.ExcelTemplateFileMapper">

    <resultMap id="BaseResultMap" type="com.jinshu.common.entity.ExcelTemplateFile">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="report_id" property="reportId"/>
        <result column="file_name" property="fileName"/>
        <result column="file_path" property="filePath"/>
        <result column="file_size" property="fileSize"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, tenant_id, report_id, file_name, file_path, file_size, created_by, created_at, updated_at
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO meta.excel_template_file
            (tenant_id, report_id, file_name, file_path, file_size, created_by, created_at, updated_at)
        VALUES
            (#{tenantId}, #{reportId}, #{fileName}, #{filePath}, #{fileSize}, #{createdBy}, #{createdAt}, #{updatedAt})
    </insert>

    <delete id="deleteById">
        DELETE FROM meta.excel_template_file WHERE id = #{id}
    </delete>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM meta.excel_template_file
        WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM meta.excel_template_file
        WHERE tenant_id = #{tenantId}
        <if test="reportId != null">
            AND report_id = #{reportId}
        </if>
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM meta.excel_template_file
        WHERE tenant_id = #{tenantId}
        <if test="reportId != null">
            AND report_id = #{reportId}
        </if>
    </select>

</mapper>
