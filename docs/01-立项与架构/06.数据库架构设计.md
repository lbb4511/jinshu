# 数据库架构设计

<!-- TOC START -->

## 目录

- [1. 技术选型决策](#1-技术选型决策)
  - [1.1 数据库选型：PostgreSQL 15](#1-1-数据库选型-postgresql-15)
  - [1.2 为什么不选 MySQL](#1-2-为什么不选-mysql)
- [2. 多租户隔离方案](#2-多租户隔离方案)
  - [2.1 隔离级别：行级隔离](#2-1-隔离级别-行级隔离)
  - [2.2 隔离实现](#2-2-隔离实现)
  - [2.3 租户上下文](#2-3-租户上下文)
  - [2.4 跨租户访问控制](#2-4-跨租户访问控制)
- [3. 命名规范（强制执行）](#3-命名规范-强制执行)
  - [3.1 表名规范](#3-1-表名规范)
  - [3.2 字段名规范](#3-2-字段名规范)
  - [3.3 索引命名规范](#3-3-索引命名规范)
- [4. 通用字段规范（所有业务表必须包含）](#4-通用字段规范-所有业务表必须包含)
- [5. 全局表清单（按模块分组）](#5-全局表清单-按模块分组)
  - [5.1 租户与权限模块](#5-1-租户与权限模块)
  - [5.2 报表元数据模块](#5-2-报表元数据模块)
  - [5.3 报表数据模块](#5-3-报表数据模块)
  - [5.4 任务调度模块](#5-4-任务调度模块)
  - [5.5 审计与安全模块](#5-5-审计与安全模块)
- [6. ER 关系总图](#6-er-关系总图)
- [7. 索引设计原则](#7-索引设计原则)
  - [7.1 必须建索引的场景](#7-1-必须建索引的场景)
  - [7.2 索引设计规范](#7-2-索引设计规范)
  - [7.3 反模式（禁止）](#7-3-反模式-禁止)
- [8. 分区与归档策略](#8-分区与归档策略)
  - [8.1 需要分区的表](#8-1-需要分区的表)
  - [8.2 归档流程](#8-2-归档流程)
  - [8.3 备份策略](#8-3-备份策略)
- [9. 数据安全](#9-数据安全)
  - [9.1 敏感字段加密](#9-1-敏感字段加密)
  - [9.2 数据库账号权限最小化](#9-2-数据库账号权限最小化)
  - [9.3 审计](#9-3-审计)
- [10. 性能目标](#10-性能目标)
- [11. DDL 变更流程](#11-ddl-变更流程)
- [12. SQL 编写规范](#12-sql-编写规范)
- [评审记录](#评审记录)

<!-- TOC END -->

**版本**：v1.0
**日期**：2026-05-12
**状态**：待评审

> ⚠️ **重要说明**：本文档是**架构级设计**，只定义原则、规范、表清单。
>
> ✅ **具体表的字段定义、索引、默认值**等详细内容，请见各模块设计文档。

---

## 1. 技术选型决策

### 1.1 数据库选型：PostgreSQL 15

| 选型维度           | 说明                                                                      |
| ------------------ | ------------------------------------------------------------------------- |
| **JSON 支持**      | PostgreSQL 的 JSONB 类型性能优异，支持 GIN 索引，完美适配报表灵活字段需求 |
| **CTE 与窗口函数** | 报表统计需要复杂 SQL，PostgreSQL 支持最完善                               |
| **分区表**         | 原生支持范围分区、列表分区，审计日志与大表可按时间分区                    |
| **全文检索**       | 内置全文检索能力，无需额外引入 ES                                         |
| **开源协议**       | PostgreSQL License，宽松友好，无商业风险                                  |

### 1.2 为什么不选 MySQL

| 对比项    | MySQL 局限              |
| --------- | ----------------------- |
| JSON 类型 | 性能较差，索引支持弱    |
| 窗口函数  | 8.0+ 才支持，功能不完整 |
| 分区表    | 限制多，子表数量上限低  |
| 乐观锁    | 需要手动实现版本字段    |

---

## 2. 多租户隔离方案

### 2.1 隔离级别：行级隔离

所有业务表统一包含 `tenant_id` 字段：

```sql
tenant_id BIGINT NOT NULL
```

### 2.2 隔离实现

**应用层**：MyBatis 拦截器自动注入 `WHERE tenant_id = ?` 条件

**数据库层**：

- 所有索引以 `tenant_id` 作为最左前缀
- 按租户分区（未来可选）

### 2.3 租户上下文

```
HTTP 请求
    ↓
Spring MVC 拦截器提取 tenant_id
    ↓
ThreadLocal 存储租户上下文
    ↓
MyBatis 拦截器自动注入 SQL 条件
```

### 2.4 跨租户访问控制

- 普通用户：只能访问当前租户数据
- 超级管理员：标注 `@SkipTenantFilter` 的方法可跨租户访问
- 审计日志：所有跨租户操作必须记录审计日志

---

## 3. 命名规范（强制执行）

### 3.1 表名规范

| 规则                | 示例                                  |
| ------------------- | ------------------------------------- |
| 全小写 + 下划线     | `report_metadata`                     |
| 模块前缀            | `report_xxx`、`task_xxx`、`audit_xxx` |
| 禁止复数形式        | ❌ `reports`，✅ `report`             |
| 关联表用 `_x_` 连接 | `user_x_role`                         |

### 3.2 字段名规范

| 规则                     | 示例                        |
| ------------------------ | --------------------------- |
| 全小写 + 下划线          | `created_at`                |
| 布尔字段用 `is_` 前缀    | `is_deleted`、`is_vip`      |
| 时间字段用 `_at` 后缀    | `created_at`、`updated_at`  |
| 外键用 `_id` 后缀        | `tenant_id`、`user_id`      |
| JSON 字段用 `_json` 后缀 | `extra_json`、`config_json` |

### 3.3 索引命名规范

| 索引类型 | 命名规则               | 示例                        |
| -------- | ---------------------- | --------------------------- |
| 主键     | `pk_表名`              | `pk_report`                 |
| 唯一索引 | `uk_表名_字段名`       | `uk_user_username`          |
| 普通索引 | `idx_表名_字段名`      | `idx_report_tenant_created` |
| 联合索引 | `idx_表名_字段1_字段2` | `idx_task_tenant_status`    |

---

## 4. 通用字段规范（所有业务表必须包含）

```sql
-- 以下字段每张业务表必须有
id              BIGSERIAL PRIMARY KEY,      -- 主键
tenant_id       BIGINT NOT NULL,            -- 租户ID
created_at      TIMESTAMP DEFAULT NOW(),    -- 创建时间
created_by      BIGINT,                     -- 创建人ID
updated_at      TIMESTAMP DEFAULT NOW(),    -- 更新时间
updated_by      BIGINT,                     -- 更新人ID
is_deleted      BOOLEAN DEFAULT FALSE       -- 软删标记
```

> ⚠️ **重要**：不允许物理删除，所有删除都是软删。审计日志表甚至不允许软删。

---

## 5. 全局表清单（按模块分组）

> 以下是所有表的清单，**具体字段定义见各模块设计文档**。

### 5.1 租户与权限模块

| 表名                | 说明            | 设计文档 |
| ------------------- | --------------- | -------- |
| `tenant`            | 租户表          | 待补充   |
| `users`             | 用户表          | 待补充   |
| `role`              | 角色表          | 待补充   |
| `permission`        | 权限表          | 待补充   |
| `user_x_role`       | 用户-角色关联表 | 待补充   |
| `role_x_permission` | 角色-权限关联表 | 待补充   |

### 5.2 报表元数据模块

| 表名              | 说明           | 设计文档                               |
| ----------------- | -------------- | -------------------------------------- |
| `report_metadata` | 报表元数据     | `03-模块设计/0x.报表元数据模块设计.md` |
| `report_schema`   | 报表表结构定义 | 待补充                                 |
| `report_workflow` | 报表审批工作流 | 待补充                                 |

### 5.3 报表数据模块

| 表名                  | 说明               | 设计文档 |
| --------------------- | ------------------ | -------- |
| `report_data`         | 报表数据宽表       | 待补充   |
| `report_data_yyyy_mm` | 按月份分区（规划） | 待补充   |

### 5.4 任务调度模块

| 表名       | 说明         | 设计文档                             |
| ---------- | ------------ | ------------------------------------ |
| `task`     | 任务主表     | `03-模块设计/01.报表导入模块设计.md` |
| `task_log` | 任务执行日志 | 待补充                               |

### 5.5 审计与安全模块

| 表名              | 说明       | 设计文档                         |
| ----------------- | ---------- | -------------------------------- |
| `audit_log`       | 审计日志   | `03-模块设计/02.审计日志设计.md` |
| `audit_root_hash` | 审计哈希根 | `03-模块设计/02.审计日志设计.md` |
| `sys_config`      | 系统配置表 | 待补充                           |
| `sys_dict`        | 数据字典表 | 待补充                           |

---

## 6. ER 关系总图

```
tenant (1) ──< (*) users
                 │
                 ├──< (*) report_metadata
                 │         │
                 │         ├──< (*) report_data
                 │         └──< (*) report_workflow
                 │
                 ├──< (*) task
                 │         └──< (*) task_log
                 │
                 └──< (*) audit_log
```

---

## 7. 索引设计原则

### 7.1 必须建索引的场景

| 场景         | 要求       |
| ------------ | ---------- |
| 外键字段     | 必须建索引 |
| 查询条件字段 | 必须建索引 |
| 排序字段     | 必须建索引 |
| 分组字段     | 必须建索引 |

### 7.2 索引设计规范

1. **租户优先**：所有业务索引以 `tenant_id` 作为最左前缀
2. **区分度优先**：区分度高的字段放在前面
3. **控制数量**：单表索引不超过 5 个
4. **避免冗余**：已有 `(a, b)` 联合索引，就不要再单独建 `(a)`

### 7.3 反模式（禁止）

❌ 禁止对低区分度字段建索引（如 `status` 只有 2-3 个值）
❌ 禁止对小表建索引（少于 1000 行）
❌ 禁止对更新频繁的字段建索引（索引维护成本高）

---

## 8. 分区与归档策略

### 8.1 需要分区的表

| 表名          | 分区策略               | 保留周期    |
| ------------- | ---------------------- | ----------- |
| `audit_log`   | 按月范围分区           | 永久保留    |
| `task_log`    | 按月范围分区           | 保留 6 个月 |
| `report_data` | 按租户列表分区（未来） | 按租户配置  |

### 8.2 归档流程

```
超过保留期的数据
        ↓
  迁移到归档表（_archive 后缀）
        ↓
  或者导出到对象存储
        ↓
  原表删除分区
```

### 8.3 备份策略

| 备份类型     | 频率          | 保留时间 |
| ------------ | ------------- | -------- |
| 全量备份     | 每日凌晨 2 点 | 30 天    |
| WAL 增量备份 | 每 5 分钟     | 7 天     |
| 关键表导出   | 每日          | 永久     |

---

## 9. 数据安全

### 9.1 敏感字段加密

| 加密级别 | 适用字段             | 实现方式                     |
| -------- | -------------------- | ---------------------------- |
| 高       | 密码、密钥、身份证号 | AES-256，密钥存在 K8s Secret |
| 中       | 手机号、邮箱         | 数据库透明加密（TDE）        |
| 低       | 普通业务数据         | 无需加密                     |

### 9.2 数据库账号权限最小化

| 账号            | 权限                   | 说明                           |
| --------------- | ---------------------- | ------------------------------ |
| `jinshu_app`    | SELECT, INSERT, UPDATE | 业务账号，禁止 DROP / TRUNCATE |
| `jinshu_audit`  | SELECT ON audit_log    | 审计员只读账号                 |
| `jinshu_backup` | SELECT 全库            | 备份专用账号                   |

### 9.3 审计

- 所有 DDL 操作必须记录
- 所有跨租户查询必须记录
- 所有高权限操作必须记录

---

## 10. 性能目标

| 指标                     | 要求    |
| ------------------------ | ------- |
| 单条简单查询             | < 10ms  |
| 100 万行表查询（带索引） | < 100ms |
| 批量插入 1000 行         | < 50ms  |
| 并发连接数               | ≥ 500   |

---

## 11. DDL 变更流程

**所有数据库变更必须严格遵循以下流程：**

```
1. 更新对应模块的设计文档
        ↓
2. 提交设计文档 CR，DBA 评审通过
        ↓
3. 编写 DDL 脚本（标注设计文档链接）
        ↓
4. 测试环境执行验证
        ↓
5. 生产环境执行
```

> ⚠️ **红线**：不允许先改数据库再补设计文档。代码评审发现 DDL 变更无对应设计文档，直接打回。

---

## 12. SQL 编写规范

详见待补充文档：`docs/07-附录/01.SQL编写规范.md`

关键要点：

- 禁止 `SELECT *`
- 禁止大事务（超过 1000 行）
- 禁止深度分页（`LIMIT 100000, 20`）
- 批量操作用 `COPY` 或 `UNION ALL`
- 深度分页用游标流式读取

---

## 评审记录

| 评审人 | 日期 | 意见 | 状态   |
| ------ | ---- | ---- | ------ |
|        |      |      | 待评审 |
